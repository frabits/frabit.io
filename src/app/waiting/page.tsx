import Waiting from '@/components/pages/waiting';

const Page = () => {
    return (
        <>
            <h1 className="sr-only">Waiting list</h1>
            <Waiting />
        </>
    );
};

export default Page;

