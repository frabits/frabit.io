'use client';

import Image from 'next/image';

import { useEffect, useRef } from 'react';

import Cards from './cards';

const Community = () => {

    return (
        <div></div>
    );
};

export { Community };
